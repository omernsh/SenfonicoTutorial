<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/BackButtonColor</item>
    </style>

    <style name="Theme.Base" parent="AppTheme">
        <item name="colorPrimaryDark">@color/PrimaryBackgroundColor</item>
        <item name="colorPrimary">@color/SecondaryBackgroundColor</item>
        <item name="colorAccent">@color/ColorBackgroundAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.Flickr" parent="Theme.Base"/>

    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:background">@color/SecondaryBackgroundColor</item>

    </style>

    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
